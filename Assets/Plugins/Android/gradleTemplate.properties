# AndroidX support
android.useAndroidX=true
android.enableJetifier=true

# Unity version-specific properties (可以被覆盖)
unityStreamingAssets=.unity3d,.ress,.resource,.obb

# Gradle daemon 缓存优化
org.gradle.jvmargs=-Xmx4096M

# 指定构建工具版本，必要时添加
android.enableR8=true
